/** Reserved: POST backend to persist plan / generate rewritten demo. */
export async function saveCustomSkinPlan({ steamid, replacements }) {
  void steamid;
  void replacements;
  // TODO: wire real API when backend is ready
  return { ok: true, stub: true };
}
